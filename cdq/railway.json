{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "chmod +x install_chrome.sh && ./install_chrome.sh"
  },
  "deploy": {
    "startCommand": "python main.py",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
